# ─────────────────────────────────────────────────────────────────────
# backend/requirements.txt  —  KOYEB DEPLOYMENT ONLY
# Do NOT add sentence-transformers, streamlit, or plotly here.
# Those are local-only tools (see requirements-local.txt).
# ─────────────────────────────────────────────────────────────────────

# Core
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.10.6
python-dotenv==1.0.1

# LangChain
langchain==0.3.0
langchain-core==0.3.0
langchain-openai==0.2.0
langchain-community==0.3.0

# Vector Search (read-only at runtime — index built locally)
faiss-cpu==1.8.0
numpy==1.26.4

# Data Processing
pandas==2.2.3

# OpenAI
openai==1.51.0
httpx==0.27.2
tiktoken==0.8.0

# Email
resend==2.4.0